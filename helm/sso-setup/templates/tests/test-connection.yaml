apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sso-setup.fullname" . }}-test-connection"
  labels:
    {{- include "sso-setup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sso-setup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
